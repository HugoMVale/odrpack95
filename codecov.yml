comment:
  layout: header, changes, diff, sunburst
coverage:
  round: down
  precision: 2
  ignore:
    - "src/linpack.f"
    - test
    - example
  status:
    patch:
      default:
        target: 20%
    project:
      default:
        target: 60%
